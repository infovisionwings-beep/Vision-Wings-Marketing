# Render Service ID: srv-d9pc628ae00c73enq150
# Live URL: https://vision-wings-marketing-fr4h.onrender.com
services:
  - type: web
    name: backend-api
    env: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 8000
      - key: DATABASE_URL
        sync: false
